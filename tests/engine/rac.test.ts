import { describe, it } from 'vitest';

describe('rac', () => {
  it.todo('r = 0 is identical to no-RAC');
  it.todo('RAC used only when the confirmed pool cannot fit');
  it.todo('slots never overbooked');
});
