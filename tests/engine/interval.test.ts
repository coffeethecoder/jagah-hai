import { describe, it } from 'vitest';

describe('interval', () => {
  it.todo('touching bookings [0,2) and [2,4) do not overlap');
  it.todo('nested and partial overlaps overlap');
  it.todo('from >= to throws');
});
