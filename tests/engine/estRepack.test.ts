import { describe, it } from 'vitest';

describe('estRepack', () => {
  it.todo('hand example fits exactly L berths');
  it.todo('throws when peakLoad > capacity');
  it.todo('no overlapping pair on a berth');
});
