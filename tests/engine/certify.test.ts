import { describe, it } from 'vitest';

describe('certify', () => {
  it.todo('forced certificate names a full segment with exactly k occupants');
  it.todo('witness is a valid assignment including the rejected booking');
});
