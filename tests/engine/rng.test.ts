import { describe, it } from 'vitest';

describe('rng', () => {
  it.todo('same seed gives the same sequence');
  it.todo('int stays within bounds');
  it.todo('weighted never picks a zero weight');
  it.todo('shuffle returns a permutation');
});
