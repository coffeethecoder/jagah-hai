import { describe, it } from 'vitest';

describe('firstFit', () => {
  it.todo('chooses the lowest free berth');
  it.todo('rejects a request that fits (strategy-induced fragmentation example)');
});
